# Tmux configuration for development services

# Enable mouse support (scroll, click, resize panes)
set -g mouse on

# Increase scrollback buffer
set -g history-limit 10000

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
